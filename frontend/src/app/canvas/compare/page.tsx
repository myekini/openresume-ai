import { VersionComparison } from "@/components/canvas/VersionComparison";

export const metadata = {
  title: "Compare Versions | OpenResume AI",
};

export default function ComparePage() {
  return <VersionComparison />;
}
