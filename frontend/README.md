# OpenResume AI — Frontend

Next.js 15 app. See the [root README](../README.md) for full setup, architecture, and API docs.

```bash
cp .env.example .env.local
npm install
npm run dev        # http://localhost:3000
```
